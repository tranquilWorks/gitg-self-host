# QA-01 targeted author review

Disposition: the missing-material defect is addressed in the source exercise.
This is an agent author/desk review, not independent C or participant evidence.

The canonical craft-foundations goal concerns the knowledge, tools, standards and
safety boundaries needed for competent work. The default practices one bounded
component: extracting supplied facts, producing a readable notice and checking it
consistently. It does not establish competence in a whole occupation. The optional
participant-chosen craft route remains separate and requires its own real inputs,
standard and appropriate supervision.

The recovered repair supplied three complete fictional briefs and matching keys.
This continuation adds the actual seven-criterion work record, concrete digital,
paper and speech inspection procedures, and a separately checked defective notice.
The same supports and format should be retained across the three attempts. A
required fact cannot be compensated for by decoration or an average rubric score;
an uninspected criterion stays unknown. Original and corrected versions remain
separate. No private record becomes an application score.

The reproducible dry-run retains all three complete plain-text outputs, a missing
contact that fails F5, and a plausible wrong date that fails F2. Each required field
also has an independently corrupted negative test, including a duplicate-contact
case. The new defect prompt requires identifying the source mismatch before the
matching check is revealed. Its supplied source brief is intentionally visible:
this is fact-checking practice, not an unaided-memory test.

The source's scored action definitions and all canonical runtime packages remain
unchanged. This educational content is still unselected for runtime compilation.
Formal scope, instructional, independent cold-start, software integration, learner,
qualified and owner reviews remain pending. A string comparison validates the
supplied labeled-text cases; it cannot assess arbitrary layouts or certify U2
for real audiences. See `dry-run.json` for the actual executed desk results.
