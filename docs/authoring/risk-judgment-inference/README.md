# Six individual competencies — 09.10–09.15

## What this checkpoint is

The owner requested the next grouping and explicitly increased its size from three to six. This source-only continuation individually authors 09.10 Risk assessment and precaution, 09.11 Practical wisdom and context, 09.12 Decision records and updating, 09.13 Statistical literacy, 09.14 Scientific reasoning and research design, and 09.15 Causal inference and intervention logic.

The six guides, twelve fresh checks, six corrective keys, and two later packets comprise **20 instructional Markdown files and 15,436 whitespace-delimited words**. A guide is not counted complete merely because it has a unique title: each has its own materials, worked example, task, deliverable, corrective guidance, accessible route, progression, bounded outcomes, and per-element scope map.

`cohort.json` identifies the exact six IDs. `fixtures.json` contains only synthetic teaching data. `SCOPE-MAP.md` maps each canonical element to its teaching and check. `SOURCES.md` records limited background support and inaccessible sources honestly. None is a production evidence contract or formal review receipt.

## Starting point and unchanged runtime

The GitHub connector confirmed main `d93532696d3ea9d04e50f47350b90fa255208511`, after PR #77. That head selects 108 tailored runtime competencies and leaves 275 pending, preserving 383 protocols and 1,151 actions. These six candidates are not compiler-selected. With the previous nine additional source candidates, the queue now contains fifteen additional authored candidates awaiting integration, not fifteen new runtime completions.

The previous groups are retained in the downloadable checkpoint for regression only: `reasoning-foundations` (09.01–09.03), `evidence-humility-forecasting` (09.04–09.06), and `systems-causes-tradeoffs` (09.07–09.09). They are not new work in this batch. Drafts #78–#79 and the earlier local checkpoint remain provenance references, not proof of current application integration.

## Use and verify

Read `learner-guide.md`, save an attempt, and then open `check-prompts.md` and its separate key. For 09.12 and 09.14, save the prospective record/protocol before opening the later packet. The reader's disclosures are educational aids, not security controls. Opening a packet early requires an honest retrospective label, not a false prospective claim.

From the bundle root, Python 3.10 or later can run the standalone checks:

```sh
python -m unittest discover -s tests -p test_m6k_risk_judgment_inference_sources.py -v
python -m unittest discover -s tests -p '*sources.py' -v
python tools/verify_bundle.py
```

The first run has 71 new source checks. The combined bundle runs 199 checks including 128 retained prior-cohort checks. These exercise original finite models, exact arithmetic, malformed inputs, privacy/chronology constraints, and visible content alignment. They do not execute Django, its compiler, historical score replay, canonical import, or actual learner review.

`REVIEW-PREVIEW.html` is an offline standalone reader. `tools/build_reader.py` regenerates it using markdown-it-py; `tools/check_reader.py` uses Playwright and system Chromium at `/usr/bin/chromium`. The environment record identifies tested versions. The browser checks exercise six guides, twelve checks, eighteen answer disclosures and two later packets, including keyboard behavior and overflow. They are not a production browser journey.

## Source-only overlay, not a full repository

Do not copy this entire bundle indiscriminately into a repository. The exact new candidate paths are in `verification/source-inventory.json`. Previous sources, screenshots, preview scripts and test logs are supporting artifacts, not a new runtime implementation. The included `contracts/active-batch.yaml` is a proposed six-ID source scope and was not applied to main.

No repository `MANIFEST.tsv` is invented from a partial checkout. On a real complete checkout, apply only the intended source payload under its approved scope, run `./scripts/verify-manifest.py --write`, and verify the resulting manifest and contract. Do not merge while required gates remain unresolved.

## Finish integration without changing meaning

Preserve the original runtime packages and hashes before selecting new content. Project these individually authored guides and their separate checks through the existing authoring schema; do not replace the catalog with a parallel Python dictionary. Keep stable protocol/action IDs, canonical parents, completion requirements, and source/recovery projections consistent.

**09.12 needs particular care:** retain its existing typed-evidence rules, neutral-code privacy boundary, observation allowlist, activation and historical replay. The lesson's synthetic record validator is deliberately not a new production schema. Active and paused practices must keep their original instructions and rules.

Regenerate the canonical packages, source/risk records, deterministic reports, current/recovery hashes and manifests. Run the repository contract, focused authoring/recovery/review tests, pilot/curriculum/competency-evidence readiness, actual Django guide and reveal checks, and required browser/Compose recovery gates. Do not reuse this standalone reader result as application evidence.

Scope, instructional, cold-start, application integration, actual learner, qualified-where-needed, and owner reviews remain separate. A source link or synthetic result does not close a human evidence requirement. Only the authoring/source portion is completed here.

## Publication and remaining access

This turn performed no GitHub write, branch creation, PR creation, merge, or deployment. GitHub read actions worked. Discovery for creation actions returned none in this turn. A direct full-checkout access attempt failed with `Could not resolve host: github.com`. No package-index failure is asserted from this turn, because it was not tested. The new source payload is preserved locally with digests and reproducible checks.

The next source IDs in domain 09 are 09.16 and 09.17; the broader backlog is not silently authorized by this six-ID task. Integration of the accumulated fifteen sources remains a distinct unfinished outcome.
