# M6K systems, causes, and tradeoffs — source checkpoint

This is the next owner-requested source grouping after draft PRs #78 and #79.
It individually authors **09.07 Systems and causal reasoning**, **09.08 Root-cause
problem solving**, and **09.09 Tradeoffs and opportunity cost**. Read each
`learner-guide.md`, save attempts from `check-prompts.md`, and then consult the
separate `check-answers.md`. 09.08 also has a delayed `test-results.md` packet.

## Counts and branch boundary

The verified main baseline remains `d93532696d3ea9d04e50f47350b90fa255208511`
(PR #77), with **108 tailored runtime competencies, 275 pending, 383 protocols,
and 1,151 actions**. The new candidate is independent of the two earlier source
branches. It does not merge them, duplicate their content in GitHub, or promote any
of these nine candidate competencies to runtime-complete or formally accepted.

The intended branch is `codex/m6k-systems-causes-tradeoffs-source-2026-09-12`.
No branch or PR was created: the publishing tool blocked the source-root write.
The three instructional directory Git objects were created and hash-matched, but
no new commit or reference was published. The complete candidate is in this bundle.
Its source changes do not modify canonical curriculum, runtime practice packages,
scoring, activation, completion rules, models, migrations, or deployment behavior.
The active-batch change describes this source scope, not runtime approval.

## What is substantively new

09.07 provides complete queue rules, a delayed rework stock, a setup-cost policy,
and a hiding policy. Learners compare a visible metric with conserved total work.
A fresh inventory case tests delayed commitments rather than copying the desk case.

09.08 provides an authoritative specification, two literal instruction cards, full
and masked input decks, four discriminating conditions, a disjoint fresh deck, and
malformed-input checks. It distinguishes containment, correct completion, and a
remaining upstream defect instead of announcing a fix from zero wrong routes.

09.09 provides complete mutually exclusive plans, protected commitments, uncertainty
and permission handling, explicit opportunity costs, and a numerical sensitivity
case. Its main choice admits different justified priorities rather than imposing a
universal preferred activity. The numerical case has a stated, limited preference rule.

## Reproduce source checks

```sh
python -m unittest discover -s tests -p test_m6k_systems_causes_tradeoffs_sources.py -v
```

The tests use only the Python standard library. They check finite fictional models,
accounting, data/answer alignment, failure cases, and source structure. They do not
run Django, the canonical compiler, score replay, production browser journeys, or
human evaluation. The helpers are test-only algorithms, not proposed production
scoring or recommendation implementations.

The conversation bundle also carries the two preceding source groups for an
explicitly labeled combined regression run. Those extra files are not part of this
planned source diff. The preview is a generated standalone reader, not a deployment.

## Integration still required

A complete local checkout remains unavailable because GitHub/package-index DNS
resolution fails in this workspace; authenticated connector access works.
`MANIFEST.tsv` is not synchronized for this source draft, so it is not merge-ready.
Regenerate the manifest with the repository's normal tooling and run the actual
contract and applicable repository gates before removing the draft boundary.

Integrate these and the preceding cohorts through the existing authoring compiler,
not by editing a displayed coverage count. Preserve the exact originals and hashes,
all protocol/action identities, parent mappings, completion rules, activation,
scoring, immutable historical replay, and active/paused instructions. Project each
competency into the existing instructional schema and retain separate checks.
Generate canonical packages, current readers, prospective recovery records, source
and risk records, and deterministic reports. Check the exact generated output.

Run the repository contract, relevant authoring/recovery/review tests,
`make pilot-check curriculum-check competency-evidence-check`, actual Django
instruction/reveal checks, the browser journeys, and Compose recovery where required.
Do not reuse a previous PR's verification result as evidence for this integration.
Resolve active-batch conflicts deliberately when combining source branches.

Formal scope, instructional, independent cold-start, integration, actual learner,
qualified-where-needed, and owner reviews stay separate. The author cannot fabricate
those receipts from model outputs. A nine-candidate source backlog does not mean
nine additional runtime-complete competencies.
