# Models, information, and execution — six authored source candidates

This cohort contains 09.16 Models, simulations, and abstraction limits; 09.17 Adversarial reasoning and information operations; 11.01 Time and energy audit; 11.02 Priority hierarchy; 11.03 Goal definition and success criteria; and 11.04 Milestones and task decomposition. Domain 10 already has its fourteen authored runtime entries and is not duplicated.

Each competency has a distinct complete guide, a different worked example, practice materials, two fresh checks, a separate corrective key, adaptations, progression, and evidence limits. Five also have a separately stored later-evidence packet. Open each learner-guide.md, preserve an attempt, then follow its directions to the later packet, check-prompts.md, and check-answers.md. File separation is an educational aid, not assessment security.

## Source publication is not runtime integration

The baseline is main d93532696d3ea9d04e50f47350b90fa255208511 after PR #77: 108 tailored runtime competencies, 275 pending, 383 protocols, and 1,151 actions. This directory is not compiler-selected. No runtime package, scoring, completion, activation, historical replay, or formal review receipt is changed.

The consolidation branch carries these six plus the preceding fifteen source candidates. See ../SOURCE-CONSOLIDATION-STATUS.md for the current publication and verification status. Older cohort README files describe their original checkpoints and must not be mistaken for the consolidation's present publication state.

## Reproducible verification

The accompanying tests/test_m6k_models_information_execution_sources.py defines 84 standalone tests. Run from the checkout:

```sh
python -m unittest discover -s tests -p 'test_m6k_models_information_execution_sources.py' -v
python -m unittest discover -s tests -p '*sources.py' -v
```

These inspect fictional case arithmetic, input handling, source dependence, time accounting, capacity, consent, goal acceptance, dependencies, and visible source/fixture alignment. They are not Django, compiler, production-scoring, historical-replay, or actual-learner tests. Current executed results belong in the consolidation evidence record; existence of a test does not imply it passed. No earlier standalone browser result is claimed as a current application pass.

cohort.json identifies the six IDs. fixtures.json is synthetic instructional data, not a production evidence contract. SCOPE-MAP.md maps individual canonical elements; SOURCES.md records background and its limits.

## Remaining integration

Use the existing authoring compiler rather than treating these review files as a second runtime catalog. Preserve original packages and hashes, stable protocol/action IDs, parent mappings, completion rules, retained typed evidence, and active/paused instructions. Generate prospective canonical packages, separate-check projections, recovery records, source/risk records, and deterministic reports. Run the repository manifest and contract checks, authoring/recovery/review regressions, pilot/curriculum/competency-evidence readiness, actual Django guide/reveal checks, browser journeys, and Compose recovery.

The retained 09.12 source has a specific privacy and typed-evidence boundary: its private decision narrative must not become automated score input. Formal scope, instructional, cold-start, actual learner, qualified-where-needed, and owner reviews stay separate. A fictional task result, research link, or source test cannot close them.
