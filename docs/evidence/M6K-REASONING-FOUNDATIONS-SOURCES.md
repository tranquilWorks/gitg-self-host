# M6K reasoning foundations — source evidence checkpoint

Date: 12 September 2026. Repository: `tranquilWorks/gitg-self-host`.
Branch: `codex/m6k-reasoning-foundations-source-2026-09-12`.
Baseline: `d93532696d3ea9d04e50f47350b90fa255208511` (merged PR #77).
Published source/test revision before this evidence-only addition:
`5c67be79e5d1715bed24aef6779eb7ac3bd771bd`.

## Delivered

Individually authored source candidates for 09.01 Observation versus interpretation,
09.02 Logic and argument structure, and 09.03 Cognitive and motivational bias.
There are nine instructional Markdown files: three complete learner guides,
three two-check prompt files, and three separate answer-and-repair files.
The nine files contain 8,534 whitespace-delimited words in the tested version.
The folder also contains an individual canonical scope map, bounded source notes,
usage/integration handoff, and executable fictional fixtures.

These are source candidates, not compiler-selected runtime packages. The baseline
remains **108/383 tailored runtime competencies and 275 pending**. No completion,
score, activation, historical replay, canonical curriculum, or formal review record
was changed. No actual learner or qualified specialist acceptance was obtained.

## Verification actually executed

`python -m unittest discover -s tests -p test_m6k_reasoning_sources.py -v`

Result: **41 tests passed**. The suite checks source structure; claim provenance;
all four Boolean assignments for the relevant argument forms; closed-inventory
premise and conclusion truth; decision arithmetic; missing-access and rest paths;
irrelevant-label and irrecoverable-cost comparisons; unchanged-choice acceptance;
and correspondence between the actual learner tables/prompts and test fixtures.
These are source checks, not a run of the repository-wide Django suite.

An initial structure test detected an inconsistent accessibility heading, which
was made explicit. Source-to-fixture review also found an ordering mismatch in
09.01 Check A's fixture. The fixture was corrected and six alignment/traceability
checks were added. The final 41-test run passes with the corrected source.

The GitHub-published bytes were compared with the tested local files using Git
blob/tree object hashes. All nine instructional files, three metadata files,
one fixture file, and the test file matched. The three directory hashes are:

- 09.01: `a6f907c2ce03bd2a1652fc8e30745f2f2e9319df`
- 09.02: `8d411fa51565d5b30449cb7fa5cdca401d49be25`
- 09.03: `33dee6b5eeaea6fabb07b04c27d0074b8444580a`
- Test blob: `be40b412a4a88acafcd44acd96b136cfadd77bde`

## Standalone preview verification

A separate, generated HTML review copy was rendered from these source files.
Headless system Chromium with Playwright checked desktop 1280 x 900 and mobile
390 x 844 views. In both views, all three guides and six fresh-check reveal controls
were present. Every one of the nine answer details (six fresh checks plus three
main-practice keys) began closed and successfully opened and closed. Competency
navigation worked and the document had no page-wide horizontal overflow.
Desktop and mobile screenshots were inspected.

The browser's file-URL policy blocked direct file navigation. The authorized,
generated HTML text was then rendered with `set_content`; browser policy was not
changed, and external network requests were blocked. This establishes in-memory
standalone reader behavior only. It is not a Django route, production asset,
application import, or deployed-browser test. The HTML, screenshots, browser check
record, and source-test log are included in the accompanying conversation artifact,
not added to the production asset pipeline.

## Explicitly not complete

The local Git checkout failed because `github.com` could not be resolved. There
was no existing full checkout or Django installation. GitHub connector file reads
and writes worked. Therefore the full compiler, runtime projection, historical
recovery/replay, Django, Ruff, contract-command, readiness, and Compose gates were
not run for this change. Earlier PR results are not reused as evidence for it.

`MANIFEST.tsv` is still the baseline manifest and does not yet account for this
source checkpoint's new files and changed active-batch byte count. This is a known
merge blocker, not a waived requirement. Synchronize it with the repository's
normal process and run the applicable gates before making this PR merge-ready.
The batch schema was inspected; the complete contract command was not executed.

No authored or human-review coverage number was advanced. Scope, instructional,
cold-start, integration, actual learner, qualified-where-needed, and owner reviews
remain distinct and pending. A fictional bias decision cannot establish the
canonical real-choice progress indicator.

## Resume here

First finish this exact three-ID cohort rather than starting another one. Obtain
a full checkout at the current branch, verify current main and source hashes,
synchronize the manifest, and run source plus contract checks. Then define the
bounded successor runtime scope, freeze the original three packages and hashes,
project these guides into the existing authoring schema, generate current packages
and review projections, and verify unchanged runtime identities and scoring/replay
semantics. Use the README's exact integration checklist. Do not merge, deploy, or
record human acceptance automatically.
