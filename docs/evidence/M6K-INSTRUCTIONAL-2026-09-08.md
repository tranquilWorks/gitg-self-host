# M6K instructional reconstruction — 2026-09-08

This follows the recovered source checkpoint on PR #67. It reconstructs
M6K-01-01 support and three related source repairs; it does not recreate the
identities of the lost commits or assert full competency acceptance.

The additive guide contract, typed compiler projection, explicit frozen
companion manifest support, authenticated renderer and view-specific exports
are implemented. Typed instructions use imported configuration protected by
the existing active/paused guard. All 383 canonical protocol packages and
1,151 action identities remain unchanged; no guide is currently selected for
production content. The companion path does not rewrite the five frozen
runtime packages. All four retained typed IDs remain explicit.

The working authoring corpus contains all 383 recovered entries; exactly 91
are explicitly selected for the current compiler. Only 21.03, 10.01 and 10.02
differ semantically from the recovered originals. The three prepared repairs
and their exact artifact fingerprints are indexed in
`docs/authoring/quality/README.md`. Each keeps formal review states pending.

Executed verification:

- `pytest tests/test_instructional_content.py tests/test_m6k_repair_drafts.py -q`:
  **17 passed in 74.57 seconds**. Includes real imported guide HTML/text/Markdown
  requests, authentication, explicit reveal, HTML escaping, no evidence writes,
  active/paused atomic refusal, canonical typed/frozen companion loading,
  independently recomputed keys, negative/partial craft outputs and exact
  per-entry scope checks.
- `pytest tests/test_tailored_practice_authoring.py tests/test_typed_evidence.py
  tests/test_full_competency_frontier.py tests/test_m6k_recovery.py -q`:
  **46 passed in 271.55 seconds**. Authoring, typed evidence, deterministic
  compilation and recovered baseline regression. These 46 and the 17 above
  are distinct executed tests, not a claim of the full application suite.
- `scripts/author_full_competency_frontier.py --check`: deterministic/current,
  91 tailored and 292 pending. Final recovery verifier: 383 exact sources,
  383 protocols, 1,151 actions, unchanged protected baseline, zero quality passes.
- Ruff checks pass. The navigator accepts the supplied advisory state and
  selects M6K-01-01, retaining its unfinished verification explicitly.

Two new Playwright journeys collect for 1280- and 390-pixel views, including
keyboard reveal and 200% text. Local Chromium installation timed out, so
collection is not counted as browser execution. No local Docker executable
is available. The recovery-only commit's hosted browser job passed, but that
does not verify this later instructional implementation. Final hosted results
and actual screenshot inspection must be checked on the published head.

M6K-01-02 revision-bound receipt enforcement and M6K-01-03 portfolio dispatch
remain pending. QA-03/04 strength and movement pathway repairs are untouched.
All 383 formal A/B/C and runtime integration reviews, and actual learner,
qualified and owner acceptance, remain pending. No participant data or live
deployment was accessed. Content recovery, a repaired draft and software tests
are separate from fully populated, professionally reviewed competencies.

Final local contract passed: environment, 1,004-file MANIFEST at that run,
Ruff formatting/lint, Django system checks and zero migration drift. All four
report generators completed successfully; the release content hash is
`4b37fa8c3f41418c12324c7b3da98c45814c72ed96034d9797b02a4bdb274645`.
The final manifest is regenerated again to include these retained evidence logs
under `docs/evidence/m6k-2026-09-08/`. Scoped diff inspection found no forbidden
paths. The updated navigator state was executed successfully and still selects
M6K-01-01. An attempted planning-test command referenced a file that the prior
plan PR did not include; no test pass is attributed to that command.
