# Three substantive runtime rewrites: 91 → 94

Base: `324e01cd9a7f5cc9a609ee857c0b5c1881e1b3e6` (merged PR #70).
Owner direction: continue substantive competency work; the runtime count had remained 91.

The compiler now selects 10.01 (The science of learning), 10.02 (Deliberate
practice), and 21.03 (Foundational craft competence), for **94 tailored / 289
rewrite-pending**. All three canonical runtime packages include the actual
instructional guide and task-specific instructions. The other 380 packages remain
byte-identical. Coverage remains distinct from the all-383 package inventory and
from every formal or human acceptance state.

See `docs/authoring/M6K-LEARNING-CRAFT-SCOPE-MAP.md` for facet-by-facet reasoning
and each competency's `runtime-learner-reader.md`, separate `runtime-check-*.md`
and exact `runtime-content-record.json`.

- 10.01 adds explicit learning-method teaching, a private session record and mixed
  question ordering. It preserves distinct worked examples, first/second attempts
  and a delayed changed-extreme-value application with independently calculated key.
- 10.02 now supplies exact spreadsheet/paper inputs, fixed C1–C4 criteria, a baseline,
  specific diagnosis, comparable fresh retry, separate checks and a concrete later
  nonadjacent-input challenge. Competent, partial and plausible incorrect fixture
  results are retained. A familiar task is maintenance, not fabricated improvement.
- 21.03 projects the complete previously checked three-brief craft practice, with
  fact accuracy, usability inspection, preserved failed outputs and a contact-change
  retest. It is a bounded fictional communication-design task.

## Compatibility

The exact 383 protocol identities, 1,151 action identities, canonical mappings,
completion rules and activation remain unchanged. All 10.02 typed evidence rules
remain exact. The established compiler updates generic primary criteria for 10.01
and 21.03 prospectively; supporting rules and stable measurement identities remain
exact, and immutable historical snapshots retain their own rules. Active/paused
import protection and replay are covered by the executed existing tests.

Historical recovery files remain byte-exact. Three byte-pinned original packages
and `runtime-projections.json` allow `recovery.py` to verify exact prospective
compiler output without rebaselining the source archive or allowing arbitrary
runtime changes. Negative tests reject changed completion rules, retained evidence
thresholds and uncompiled instructions. The original repair records now test their
historical source snapshots, not a permanent prohibition on future content work.

The current program disposition map had bound the old 91/292 test assertion.
Its exact previous version is retained under `m6k-learning-craft/`; supported
software test evidence and dependent proofs were renewed after execution. No
competency review ledger entry or human acceptance was added. The formal program
queue still requests its own repair/review dispositions independently of runtime
authored coverage.

## Executed checks

Evidence files below are in `docs/evidence/m6k-learning-craft/`.

| Check | Result and evidence |
| --- | --- |
| Content, compiler projection, recovery, immutable rules, existing import/replay and review tests | 104 passed in the initial 107-test run. Three new view tests failed because their test URL omitted the namespace; corrected without a product-code change, then all three passed in the focused rerun. Retained `regression-initial.txt` and `runtime-views.txt`; no failures remain unresolved |
| Continuation regression | 30 passed; `continuation-tests.txt` |
| Total distinct focused tests verified | 137 across those runs; not a claim that the full repository suite ran locally |
| Actual authenticated guide/reveal pages | All three competencies passed the focused view tests; these are Django client checks, not browser screenshots |
| Exact deterministic compiler | 94/383 authored, 289 pending; `compiler.txt` |
| Recovery and current review report | Recovery passes with explicit prospective content; review check exits 0, no invalid current receipts and no formal acceptance inferred |
| Scope and neighbors | Exactly three runtime packages changed; other 380 byte-identical, including all frozen packages; `scope-check.json` |
| Ruff, Django, migration drift | Passed repository contract; `contract.txt` |
| Isolated pilot readiness | Passed with 383 protocols / 1,151 actions; `pilot.txt` |
| Isolated curriculum readiness | Passed, deterministic reports current; `curriculum.txt` |
| Isolated competency-evidence readiness | Passed; exact v1/score-state software replay and retained specialist gaps; `competency-evidence.txt` |
| Generated control reports | Coverage, risk, originality, governance, review queue, competency evidence and composite catalog regenerated; `reports.txt` |

The source arithmetic desk run is a restricted formula-selection simulation, not
an executed spreadsheet application or an actual learner. A week of retention,
physical notice production, live audience access and independent cold-start review
were not simulated into acceptance. Qualified, actual-learner and owner dimensions
remain pending. Browser/Compose and the full hosted aggregate gate remain required
before a pilot-bound merge; Docker and a working local browser installation are
unavailable here. This draft publishes content for review, with no deployment,
participant exposure or new scoring authorization.
